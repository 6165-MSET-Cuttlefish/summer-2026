/**
 * Top-level build file for ftc_app project.
 *
 * It is extraordinarily rare that you will ever need to edit this file.
 */

buildscript {
    // Pinned Sloth commit on the 6165 fork. Bump alongside any future audit
    // follow-ups in https://github.com/6165-MSET-Cuttlefish/Sloth.
    ext.slothRef = '9bcb914'
    // Pinned slothboard commit on the 6165 fork. The published v1.0.0 tag
    // predates the Sloth-fork swap; bump to a newer tag once one is cut.
    ext.slothboardRef = 'edd4aca'
    ext {
        // Single source of truth for the Kotlin Gradle plugin version (used in the
        // classpath below). Kotlin 2.4.0 fully supports Gradle 7.6.3–9.5.0, so the
        // pinned Gradle 8.9 is within range.
        kotlin_version = '2.4.0'
    }
    repositories {
        mavenCentral()
        google()
        maven { url 'https://jitpack.io' }
    }
    dependencies {
        // Note for FTC Teams: Do not modify this yourself.
        classpath 'com.android.tools.build:gradle:8.7.0'
        classpath "com.github.6165-MSET-Cuttlefish.Sloth:Load:${slothRef}"
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
    }
}

// This is now required because aapt2 has to be downloaded from the
// google() repository beginning with version 3.2 of the Android Gradle Plugin
allprojects {
    repositories {
        mavenCentral()
        google()
        // Sloth runtime fork (and its Load plugin classpath above).
        maven { url 'https://jitpack.io' }
        // Sloth's transitive Sinister dependency lives here.
        maven { url 'https://repo.dairy.foundation/releases' }
    }
}

repositories {
    mavenCentral()
}
